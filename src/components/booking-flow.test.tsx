import React from "react"; import { render,screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe,expect,it } from "vitest"; import { BookingFlow } from "./booking-flow"; import { ToastProvider } from "./toast";
describe("booking flow",()=>{it("renders a move and advances",async()=>{render(<ToastProvider><BookingFlow service="move"/></ToastProvider>);expect(await screen.findByText("Where are you moving?")).toBeInTheDocument();await userEvent.click(screen.getByRole("button",{name:/continue/i}));expect(screen.getByText("When works best?")).toBeInTheDocument()});it("labels local-only draft behavior",async()=>{render(<ToastProvider><BookingFlow service="remove"/></ToastProvider>);expect(await screen.findByText(/Draft saved only in this browser/i)).toBeInTheDocument()})});
