import { EmptyState } from "@/components/empty-state"; import { RoleShell } from "@/components/role-shell"; export default function Page(){return <RoleShell role="crew"><p className="eyebrow">Crew workspace</p><h1 className="mt-2 mb-8 text-4xl font-black">Today’s field work</h1><EmptyState title="No crew assignments yet" copy="Assigned stops, scope notes, photos, status controls, and completion records will appear here after provider onboarding and secure dispatch are connected."/></RoleShell>}
